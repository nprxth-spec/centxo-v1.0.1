'use client';

/**
 * AutoCreateContent - Full-featured AI-powered ad creation
 * Embeds the existing CreateAdsPage functionality
 */

import CreateAdsPage from '@/app/(app)/create-ads/page';

export default function AutoCreateContent() {
  // Render the full create-ads page within this container
  return <CreateAdsPage />;
}
